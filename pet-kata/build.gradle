plugins {
    id 'java'
    id 'kotlin'
}

dependencies {
    implementation(project(':pet-model'))
    testImplementation testFixtures(project(':pet-model'))
}

config.kotlin.configureEach {
    kotlinOptions {
        jvmTarget = '11'
    }
}
